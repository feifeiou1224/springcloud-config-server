eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true

spring:
  application:
    name: PRODUCT-PROVIDER
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springcloud_sell?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT
    username: root
    password: 1234
  rabbitmq:
    host: 192.168.1.223
    port: 5672
    username: guest
    password: guest

server:
  port: 8080
