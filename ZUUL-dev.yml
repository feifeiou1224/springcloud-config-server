spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
  application:
    name: ZUUL

      
server:
  port: 9000
  
management:
  endpoint:
    web:
      exposure:
        include: "*"
