eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true

spring:
  application:
    name: PRODUCT-PROVIDER
  rabbitmq:
    host: 192.168.1.223
    port: 5672
    username: guest
    password: guest

server:
  port: 8081
